{
  "name": "bpm-detector",
  "version": "1.0.0",
  "description": "Real-time BPM detection and live tracking",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "pretest": "./setup.sh"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "jest": "^29.7.0",
    "gh-pages": "^6.0.0"
  }
}
